DJANGO_SETTINGS_MODULE=config.settings.local

# Perfil local para trabajar con PostgreSQL.
# Copiar como .env y completar sólo en la máquina local.
# DB_PASSWORD es un secreto local: no debe quedar en Git ni en capturas.
DB_ENGINE=postgres
DB_NAME=uni2
DB_USER=postgres
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432

# --- Storage externo (S3 / R2) ------------------------------------------------
# Opcional. Si no se setean, las fotos se guardan en el filesystem local.
# AWS_ACCESS_KEY_ID es sensible y AWS_SECRET_ACCESS_KEY es secreto.
# Usar un bucket de desarrollo, nunca el bucket de Production.
# AWS_STORAGE_BUCKET_NAME=uni2-media
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_ENDPOINT_URL=https://<accountid>.r2.cloudflarestorage.com
# AWS_S3_REGION_NAME=auto

# Nota:
# Si la instalación local usa un socket de PostgreSQL, se puede dejar
# DB_HOST vacío.
